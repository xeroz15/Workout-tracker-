import { useState, useEffect } from "react";

const muscleGroups = {
  Chest: {
    icon: "💪",
    color: "#e63946",
    exercises: [
      "Flat Bench Chest Press",
      "Inclined Chest Press",
      "Pec Dec Fly",
      "Cable Fly for Lower Chest",
    ],
  },
  Back: {
    icon: "🏋️",
    color: "#f4a261",
    exercises: [
      "Lat Cable Pull",
      "Seated Row Close Grip",
      "Wide Grip Machine Row",
      "Shrugs",
    ],
  },
  Triceps: {
    icon: "⚡",
    color: "#2ec4b6",
    exercises: [
      "Cable Rope Push Down",
      "Cable Rope Over Head",
      "Single Hand Cable D-Grip Push Down",
    ],
  },
  Biceps: {
    icon: "🔥",
    color: "#e76f51",
    exercises: [
      "Preacher Curl",
      "Barbell Curl",
      "Inclined Dumbbell Curl",
    ],
  },
  Shoulders: {
    icon: "🎯",
    color: "#a8dadc",
    exercises: [
      "Shoulder Press Dumbbell",
      "Shoulder Press Barbell",
      "Cable Lateral Raises",
      "Reverse Pec Dec",
    ],
  },
  Forearms: {
    icon: "🤜",
    color: "#c77dff",
    exercises: [
      "Cable Straight Bar Pronated Grip Wrist Curl (Front)",
      "Cable Straight Bar Supinated Grip Wrist Curl (Back)",
      "Hammer Curl Preacher",
      "Dead Hand",
    ],
  },
  Legs: {
    icon: "🦵",
    color: "#06d6a0",
    exercises: [
      "Squat",
      "Leg Press (Wide Leg Position)",
      "Lunges",
      "Single Leg Curl",
      "Calf",
    ],
  },
  Abs: {
    icon: "⬡",
    color: "#ffd60a",
    exercises: [
      "Crunches",
      "Leg Raises",
      "Plank",
      "Russian Twists",
    ],
  },
};

const defaultWeekPlan = {
  Sun: "",
  Mon: "",
  Tue: "",
  Wed: "",
  Thu: "",
  Fri: "",
  Sat: "",
};

export default function WorkoutTracker() {
  const [screen, setScreen] = useState("splash");
  const [activeTab, setActiveTab] = useState("home");
  const [selectedMuscle, setSelectedMuscle] = useState(null);
  const [weights, setWeights] = useState({});
  const [weekPlan, setWeekPlan] = useState(defaultWeekPlan);
  const [editingDay, setEditingDay] = useState(null);
  const [notification, setNotification] = useState("");

  useEffect(() => {
    const timer = setTimeout(() => setScreen("home"), 2400);
    return () => clearTimeout(timer);
  }, []);

  const getWeight = (muscle, exercise) => {
    return weights[`${muscle}__${exercise}`] || "";
  };

  const setWeight = (muscle, exercise, val) => {
    setWeights((prev) => ({
      ...prev,
      [`${muscle}__${exercise}`]: val,
    }));
  };

  const showNotif = (msg) => {
    setNotification(msg);
    setTimeout(() => setNotification(""), 1800);
  };

  // ─── SPLASH ───────────────────────────────────────────────────
  if (screen === "splash") {
    return (
      <div style={styles.splash}>
        <div style={styles.splashGlow} />
        <div style={styles.splashContent}>
          <div style={styles.logoRing}>
            <svg width="72" height="72" viewBox="0 0 72 72" fill="none">
              <circle cx="36" cy="36" r="34" stroke="#e63946" strokeWidth="2.5" strokeDasharray="6 3" />
              <circle cx="36" cy="36" r="24" fill="#1a1a2e" stroke="#e63946" strokeWidth="1.5" />
              {/* dumbbell icon */}
              <rect x="20" y="33" width="32" height="6" rx="3" fill="#e63946" />
              <rect x="14" y="28" width="8" height="16" rx="3" fill="#e63946" />
              <rect x="50" y="28" width="8" height="16" rx="3" fill="#e63946" />
              <rect x="11" y="31" width="5" height="10" rx="2.5" fill="#c1121f" />
              <rect x="56" y="31" width="5" height="10" rx="2.5" fill="#c1121f" />
            </svg>
          </div>
          <div style={styles.splashTitle}>WORKOUT</div>
          <div style={styles.splashSub}>TRACKER</div>
          <div style={styles.splashLine} />
          <div style={styles.splashTagline}>FORGE YOUR STRENGTH</div>
        </div>
      </div>
    );
  }

  // ─── EXERCISE DETAIL ──────────────────────────────────────────
  if (selectedMuscle) {
    const group = muscleGroups[selectedMuscle];
    return (
      <div style={styles.app}>
        <div style={styles.detailHeader}>
          <button style={styles.backBtn} onClick={() => setSelectedMuscle(null)}>
            ← BACK
          </button>
          <div style={{ display: "flex", alignItems: "center", gap: 10 }}>
            <span style={{ fontSize: 26 }}>{group.icon}</span>
            <span style={{ ...styles.detailTitle, color: group.color }}>{selectedMuscle.toUpperCase()}</span>
          </div>
          <div style={{ width: 60 }} />
        </div>
        <div style={styles.detailAccent(group.color)} />

        <div style={styles.exerciseList}>
          {group.exercises.map((ex, i) => (
            <div key={ex} style={styles.exerciseCard(group.color)}>
              <div style={styles.exNum(group.color)}>{String(i + 1).padStart(2, "0")}</div>
              <div style={styles.exName}>{ex}</div>
              <div style={styles.weightRow}>
                <span style={styles.weightLabel}>KG</span>
                <input
                  type="number"
                  min="0"
                  style={styles.weightInput(group.color)}
                  value={getWeight(selectedMuscle, ex)}
                  onChange={(e) => setWeight(selectedMuscle, ex, e.target.value)}
                  placeholder="0"
                />
                <button
                  style={styles.saveBtn(group.color)}
                  onClick={() => showNotif(`Saved ${ex}!`)}
                >
                  ✓
                </button>
              </div>
            </div>
          ))}
        </div>

        {notification && <div style={styles.toast}>{notification}</div>}
      </div>
    );
  }

  // ─── MAIN APP ─────────────────────────────────────────────────
  return (
    <div style={styles.app}>
      {/* Header */}
      <div style={styles.header}>
        <div style={styles.headerLogo}>
          <svg width="28" height="28" viewBox="0 0 72 72" fill="none">
            <rect x="20" y="33" width="32" height="6" rx="3" fill="#e63946" />
            <rect x="14" y="28" width="8" height="16" rx="3" fill="#e63946" />
            <rect x="50" y="28" width="8" height="16" rx="3" fill="#e63946" />
            <rect x="11" y="31" width="5" height="10" rx="2.5" fill="#c1121f" />
            <rect x="56" y="31" width="5" height="10" rx="2.5" fill="#c1121f" />
          </svg>
          <span style={styles.headerTitle}>WORKOUT TRACKER</span>
        </div>
      </div>

      {/* Tab Content */}
      <div style={styles.content}>
        {activeTab === "home" && (
          <>
            <div style={styles.sectionLabel}>MUSCLE GROUPS</div>
            <div style={styles.grid}>
              {Object.entries(muscleGroups).map(([name, data]) => (
                <button
                  key={name}
                  style={styles.muscleCard(data.color)}
                  onClick={() => setSelectedMuscle(name)}
                >
                  <div style={styles.cardGlow(data.color)} />
                  <span style={styles.cardIcon}>{data.icon}</span>
                  <span style={styles.cardName}>{name.toUpperCase()}</span>
                  <span style={styles.cardCount}>{data.exercises.length} exercises</span>
                </button>
              ))}
            </div>
          </>
        )}

        {activeTab === "plan" && (
          <>
            <div style={styles.sectionLabel}>WEEKLY SPLIT</div>
            <div style={styles.weekGrid}>
              {Object.entries(weekPlan).map(([day, val]) => (
                <div key={day} style={styles.dayCard}>
                  <div style={styles.dayLabel}>{day.toUpperCase()}</div>
                  {editingDay === day ? (
                    <textarea
                      autoFocus
                      style={styles.dayTextarea}
                      value={val}
                      onChange={(e) =>
                        setWeekPlan((p) => ({ ...p, [day]: e.target.value }))
                      }
                      onBlur={() => setEditingDay(null)}
                      placeholder={`Enter your ${day} workout...`}
                    />
                  ) : (
                    <div
                      style={styles.dayContent}
                      onClick={() => setEditingDay(day)}
                    >
                      {val || <span style={styles.restDay}>REST / TAP TO EDIT</span>}
                    </div>
                  )}
                </div>
              ))}
            </div>
          </>
        )}
      </div>

      {/* Bottom Nav */}
      <div style={styles.bottomNav}>
        <button
          style={styles.navBtn(activeTab === "home")}
          onClick={() => setActiveTab("home")}
        >
          <span style={{ fontSize: 20 }}>🏠</span>
          <span style={styles.navLabel}>HOME</span>
        </button>
        <button
          style={styles.navBtn(activeTab === "plan")}
          onClick={() => setActiveTab("plan")}
        >
          <span style={{ fontSize: 20 }}>📅</span>
          <span style={styles.navLabel}>WEEKLY PLAN</span>
        </button>
      </div>

      {notification && <div style={styles.toast}>{notification}</div>}
    </div>
  );
}

// ─── STYLES ───────────────────────────────────────────────────────
const styles = {
  splash: {
    width: "100%",
    minHeight: "100vh",
    background: "#0a0a12",
    display: "flex",
    alignItems: "center",
    justifyContent: "center",
    position: "relative",
    overflow: "hidden",
    fontFamily: "'Bebas Neue', 'Impact', sans-serif",
  },
  splashGlow: {
    position: "absolute",
    width: 400,
    height: 400,
    borderRadius: "50%",
    background: "radial-gradient(circle, rgba(230,57,70,0.18) 0%, transparent 70%)",
    top: "50%",
    left: "50%",
    transform: "translate(-50%,-50%)",
    animation: "pulse 2s ease-in-out infinite",
  },
  splashContent: {
    display: "flex",
    flexDirection: "column",
    alignItems: "center",
    gap: 6,
    zIndex: 2,
    animation: "fadeIn 0.8s ease",
  },
  logoRing: {
    marginBottom: 10,
    filter: "drop-shadow(0 0 18px rgba(230,57,70,0.6))",
    animation: "spin 8s linear infinite",
  },
  splashTitle: {
    fontSize: 64,
    letterSpacing: 12,
    color: "#fff",
    lineHeight: 1,
    textShadow: "0 0 30px rgba(230,57,70,0.5)",
  },
  splashSub: {
    fontSize: 64,
    letterSpacing: 18,
    color: "#e63946",
    lineHeight: 1,
    textShadow: "0 0 40px rgba(230,57,70,0.8)",
  },
  splashLine: {
    width: 120,
    height: 2,
    background: "linear-gradient(90deg, transparent, #e63946, transparent)",
    margin: "12px 0",
  },
  splashTagline: {
    fontSize: 14,
    letterSpacing: 6,
    color: "#888",
  },

  app: {
    width: "100%",
    minHeight: "100vh",
    background: "#0d0d1a",
    fontFamily: "'Bebas Neue', 'Impact', sans-serif",
    color: "#fff",
    display: "flex",
    flexDirection: "column",
    position: "relative",
  },
  header: {
    padding: "14px 20px",
    background: "linear-gradient(180deg, #111122 0%, transparent 100%)",
    borderBottom: "1px solid rgba(230,57,70,0.2)",
    display: "flex",
    alignItems: "center",
    justifyContent: "center",
    position: "sticky",
    top: 0,
    zIndex: 10,
    backdropFilter: "blur(10px)",
  },
  headerLogo: {
    display: "flex",
    alignItems: "center",
    gap: 10,
  },
  headerTitle: {
    fontSize: 22,
    letterSpacing: 5,
    color: "#fff",
    textShadow: "0 0 20px rgba(230,57,70,0.4)",
  },

  content: {
    flex: 1,
    padding: "20px 16px 90px",
    overflowY: "auto",
  },
  sectionLabel: {
    fontSize: 13,
    letterSpacing: 5,
    color: "#555",
    marginBottom: 16,
    paddingLeft: 4,
  },

  grid: {
    display: "grid",
    gridTemplateColumns: "1fr 1fr",
    gap: 14,
  },
  muscleCard: (color) => ({
    background: "linear-gradient(135deg, #141428 0%, #1a1a30 100%)",
    border: `1px solid ${color}33`,
    borderRadius: 14,
    padding: "20px 14px",
    display: "flex",
    flexDirection: "column",
    alignItems: "flex-start",
    gap: 6,
    cursor: "pointer",
    position: "relative",
    overflow: "hidden",
    transition: "transform 0.15s, box-shadow 0.15s",
    boxShadow: `0 4px 20px ${color}15`,
    textAlign: "left",
  }),
  cardGlow: (color) => ({
    position: "absolute",
    top: -20,
    right: -20,
    width: 80,
    height: 80,
    borderRadius: "50%",
    background: `radial-gradient(circle, ${color}25 0%, transparent 70%)`,
    pointerEvents: "none",
  }),
  cardIcon: {
    fontSize: 30,
    lineHeight: 1,
  },
  cardName: {
    fontSize: 20,
    letterSpacing: 2,
    color: "#fff",
    fontWeight: 700,
  },
  cardCount: {
    fontSize: 11,
    letterSpacing: 1,
    color: "#666",
    fontFamily: "monospace",
  },

  detailHeader: {
    display: "flex",
    alignItems: "center",
    justifyContent: "space-between",
    padding: "14px 16px",
    borderBottom: "1px solid rgba(255,255,255,0.06)",
    position: "sticky",
    top: 0,
    background: "#0d0d1a",
    zIndex: 10,
  },
  backBtn: {
    background: "none",
    border: "1px solid rgba(255,255,255,0.12)",
    color: "#aaa",
    padding: "6px 12px",
    borderRadius: 6,
    fontSize: 12,
    letterSpacing: 2,
    cursor: "pointer",
    fontFamily: "'Bebas Neue', sans-serif",
  },
  detailTitle: {
    fontSize: 24,
    letterSpacing: 4,
  },
  detailAccent: (color) => ({
    height: 2,
    background: `linear-gradient(90deg, ${color}, transparent)`,
    marginBottom: 20,
  }),

  exerciseList: {
    padding: "0 16px 90px",
    display: "flex",
    flexDirection: "column",
    gap: 12,
  },
  exerciseCard: (color) => ({
    background: "linear-gradient(135deg, #141428, #181830)",
    border: `1px solid ${color}22`,
    borderRadius: 12,
    padding: "16px",
    display: "flex",
    flexDirection: "column",
    gap: 12,
    boxShadow: `0 2px 12px ${color}10`,
  }),
  exNum: (color) => ({
    fontSize: 11,
    letterSpacing: 2,
    color: color,
    fontFamily: "monospace",
  }),
  exName: {
    fontSize: 17,
    letterSpacing: 1.5,
    color: "#eee",
    lineHeight: 1.2,
  },
  weightRow: {
    display: "flex",
    alignItems: "center",
    gap: 10,
    marginTop: 4,
  },
  weightLabel: {
    fontSize: 12,
    letterSpacing: 2,
    color: "#555",
  },
  weightInput: (color) => ({
    width: 80,
    background: "#0a0a18",
    border: `1px solid ${color}44`,
    borderRadius: 6,
    color: "#fff",
    padding: "8px 10px",
    fontSize: 18,
    fontFamily: "monospace",
    outline: "none",
    textAlign: "center",
  }),
  saveBtn: (color) => ({
    background: `${color}22`,
    border: `1px solid ${color}55`,
    color: color,
    borderRadius: 6,
    width: 36,
    height: 36,
    cursor: "pointer",
    fontSize: 16,
    display: "flex",
    alignItems: "center",
    justifyContent: "center",
  }),

  weekGrid: {
    display: "flex",
    flexDirection: "column",
    gap: 10,
  },
  dayCard: {
    background: "#141428",
    border: "1px solid rgba(255,255,255,0.07)",
    borderRadius: 12,
    padding: "14px 16px",
    display: "flex",
    gap: 14,
    alignItems: "flex-start",
  },
  dayLabel: {
    fontSize: 20,
    letterSpacing: 3,
    color: "#e63946",
    minWidth: 42,
    paddingTop: 2,
  },
  dayContent: {
    flex: 1,
    fontSize: 14,
    letterSpacing: 1,
    color: "#ccc",
    cursor: "pointer",
    lineHeight: 1.5,
    minHeight: 28,
    fontFamily: "monospace",
    whiteSpace: "pre-wrap",
  },
  restDay: {
    color: "#333",
    fontSize: 11,
    letterSpacing: 2,
  },
  dayTextarea: {
    flex: 1,
    background: "#0a0a18",
    border: "1px solid rgba(230,57,70,0.3)",
    borderRadius: 8,
    color: "#fff",
    padding: "8px 10px",
    fontSize: 14,
    fontFamily: "monospace",
    outline: "none",
    minHeight: 80,
    width: "100%",
    resize: "vertical",
    letterSpacing: 1,
    lineHeight: 1.5,
  },

  bottomNav: {
    position: "fixed",
    bottom: 0,
    left: 0,
    right: 0,
    display: "flex",
    background: "rgba(10,10,20,0.97)",
    borderTop: "1px solid rgba(230,57,70,0.2)",
    backdropFilter: "blur(12px)",
    zIndex: 20,
  },
  navBtn: (active) => ({
    flex: 1,
    padding: "12px 0",
    display: "flex",
    flexDirection: "column",
    alignItems: "center",
    gap: 4,
    background: active ? "rgba(230,57,70,0.08)" : "none",
    border: "none",
    borderTop: active ? "2px solid #e63946" : "2px solid transparent",
    cursor: "pointer",
    color: active ? "#e63946" : "#444",
    transition: "all 0.2s",
  }),
  navLabel: {
    fontSize: 9,
    letterSpacing: 2,
    fontFamily: "'Bebas Neue', sans-serif",
  },

  toast: {
    position: "fixed",
    bottom: 80,
    left: "50%",
    transform: "translateX(-50%)",
    background: "rgba(230,57,70,0.9)",
    color: "#fff",
    padding: "10px 24px",
    borderRadius: 20,
    fontSize: 13,
    letterSpacing: 2,
    zIndex: 999,
    fontFamily: "'Bebas Neue', sans-serif",
    boxShadow: "0 4px 20px rgba(230,57,70,0.4)",
    animation: "fadeIn 0.2s ease",
  },
};
